import { R3CompiledExpression } from '@angular/compiler/src/render3/util';
import { R3DirectiveMetadata } from '@angular/compiler/src/render3/view/api';
import { DefinitionMap } from '@angular/compiler/src/render3/view/util';
import { R3DeclareDirectiveMetadata } from '@angular/compiler/src/render3/partial/api';
/**
 * Compile a directive declaration defined by the `R3DirectiveMetadata`.
 */
export declare function compileDeclareDirectiveFromMetadata(meta: R3DirectiveMetadata): R3CompiledExpression;
/**
 * Gathers the declaration fields for a directive into a `DefinitionMap`. This allows for reusing
 * this logic for components, as they extend the directive metadata.
 */
export declare function createDirectiveDefinitionMap(meta: R3DirectiveMetadata): DefinitionMap<R3DeclareDirectiveMetadata>;
